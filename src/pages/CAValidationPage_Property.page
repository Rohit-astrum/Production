<apex:page sidebar="false" tabStyle="Property__c"
	controller="QAS_NA.CARedirectToValidationPageController"
	action="{!DoAddressValidation}">
	<apex:form >
		<QAS_NA:CAAddressesComponent Results="{!Results}"
			Results1="{!Results1}" HasResults1="{!HasResults1}"
			Results2="{!Results2}" HasResults2="{!HasResults2}"
			HasErrorOccured="{!HasErrorOccured}" sObjectName="{!SObjectLabel}"
			ExecuteAddressCorrectionAndMoveNext="{!ExecuteAddressCorrectionAndMoveNext}"
			IsInConsole="{!IsInConsole}" WarningMessage="{!WarningMessage}">
		</QAS_NA:CAAddressesComponent>
	</apex:form>
</apex:page>